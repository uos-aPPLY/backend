# 이 파일은 필요한 환경 변수의 목록입니다.
# 실제 값으로 채워진 .env 파일을 생성하여 사용하세요.
# 실제 .env 파일은 절대로 Git에 커밋하지 마세요! (.gitignore에 .env 추가)

# Docker Compose DB Service
MYSQL_DATABASE=diarypic_db
MYSQL_ROOT_PASSWORD= # 여기에 실제 DB 루트 비밀번호 입력

# Backend Application (application.yml에서 참조)
DB_USERNAME=root
DB_PASSWORD= # 여기에 실제 DB 사용자 비밀번호 입력 (위 MYSQL_ROOT_PASSWORD와 같을 수 있음)

# AWS Credentials
AWS_ACCESS_KEY_ID= # 여기에 AWS Access Key ID 입력
AWS_SECRET_ACCESS_KEY= # 여기에 AWS Secret Key 입력

# OAuth2 Credentials
KAKAO_CLIENT_ID= # 여기에 Kakao Client ID 입력
# KAKAO_CLIENT_SECRET= # 카카오 Client Secret (필요하다면 추가)
GOOGLE_CLIENT_ID= # 여기에 Google Client ID 입력
GOOGLE_CLIENT_SECRET= # 여기에 Google Client Secret 입력

# JWT Secret Key
JWT_SECRET= # 여기에 JWT Secret Key 입력

# Base URL for Redirect URIs etc.
BASE_URL=http://localhost:8080 # 로컬 테스트 시 또는 실제 배포 URL

# 기타 필요한 환경 변수 추가 가능